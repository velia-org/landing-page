"use client";
import { useState } from "react"; import { ProductCard } from "@/components/ProductCard"; import { products } from "@/data/products";
export default function Joyas(){const [filter,setFilter]=useState('Todas');const collections=['Todas',...Array.from(new Set(products.map(p=>p.collection)))];const list=filter==='Todas'?products:products.filter(p=>p.collection===filter);return <section className="shopPage section"><div className="pageHeading"><span className="eyebrow">VELIA · JOYAS</span><h1>Piezas para <em>quedarse.</em></h1><p>Encuentra una joya por cómo se ve, por lo que representa o simplemente porque sentiste que era para ti.</p></div><div className="filterRow">{collections.map(c=><button data-cursor="hover" key={c} onClick={()=>setFilter(c)} className={filter===c?'active':''}>{c}</button>)}</div><div className="productGrid">{list.map(p=><ProductCard key={p.slug} product={p}/>)}</div></section>}
