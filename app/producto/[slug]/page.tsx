import Image from "next/image"; import { notFound } from "next/navigation"; import { products, formatPrice } from "@/data/products"; import { ProductPurchase } from "@/components/ProductPurchase";
export function generateStaticParams(){return products.map(p=>({slug:p.slug}));}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();return <section className="pdp"><div className="pdpGallery"><div className="pdpImage"><Image src={p.image} alt={p.imageAlt} fill priority sizes="(max-width:850px) 100vw, 58vw"/></div><div className="pdpDetailArt"><Image src="/images/detail.svg" alt="Detalle editorial de la pieza" fill sizes="50vw"/></div></div><aside className="pdpInfo"><span className="eyebrow">{p.collection}</span><h1>{p.name}</h1><div className="pdpPrice">{formatPrice(p.price)}</div><p className="pdpMaterial">{p.material}</p><ProductPurchase product={p}/><div className="pdpMeaning"><span>SU SIGNIFICADO</span><h2>{p.meaning}</h2><p>Una pieza pensada para acompañarte sin pedir protagonismo: delicada, cotidiana y con una historia que puedes hacer tuya.</p></div><details><summary>Detalles y cuidado</summary><p>Evita perfumes directos y humedad prolongada. Guarda tu pieza separada de otras joyas.</p></details><details><summary>Entrega y cambios</summary><p>Información demostrativa. Conecta aquí tus políticas reales antes de producción.</p></details></aside></section>}
