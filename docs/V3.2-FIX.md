# VELIA V3.2 — corrección verificada

## Assets oficiales
- Los SVG oficiales ya no se usan como máscaras CSS.
- `Wordmark` y `Monogram` renderizan archivos SVG reales mediante `<img>`.
- Los originales borgoña se copian sin modificar desde los assets entregados por el usuario.
- Las variantes crema cambian únicamente el `fill` de #5B1F33 a #F6EFE8 y preservan exactamente los mismos paths/viewBox.
- `app/icon.svg` es el favicon oficial entregado por el usuario.

## Footer
- `botanicalFooterWrap`: 150svh (138svh en móvil) para conservar recorrido de revelado.
- `botanicalFooterStage`: `position: sticky; top: 0; height: 100svh`.
- La escena queda fijada durante el recorrido y continúa ocupando todo el viewport en el final físico del documento.
- Se eliminó el `min-height:720px` móvil que podía permitir desplazar la composición fuera del viewport.
- A progreso 1, escena y textos permanecen visibles; no existe un tramo negro vacío posterior.
